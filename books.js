// Wadah (Meja Persiapan) untuk menyimpan kumpulan data buku.
// Kita menggunakan let (bukan const) seandainya nanti kita perlu merombak ulang isi raknya.
// Tapi direkomendasikan menggunakan const untuk array, karena kita hanya akan melakukan push().
// Untuk amannya, kita ikuti standar dengan const.

const books = [];

export default books;